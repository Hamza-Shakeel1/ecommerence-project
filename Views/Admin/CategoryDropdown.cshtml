@model List<Category>


   
<select name="Cart_Id" > <option hidden>select any category</option>
    @foreach(var cat in Model)
    {
        <option value="@cat.category_Id">@cat.Category_Name</option>
    }
    
</select>